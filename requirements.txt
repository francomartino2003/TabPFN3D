# Core dependencies
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
statsmodels>=0.14.0
scikit-learn>=1.3.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Time series
aeon>=0.4.0

# Data handling
tqdm>=4.65.0
pyarrow>=12.0.0

# Jupyter
jupyter>=1.0.0
ipykernel>=6.0.0

# TabPFN
tabpfn>=6.0.0

# PyTorch with CUDA (install separately with):
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
# Or for CPU only:
# pip install torch torchvision

# Temporal Encoder dependencies (04_temporal_encoder)
einops>=0.7.0
wandb>=0.15.0  # Optional: for experiment tracking

